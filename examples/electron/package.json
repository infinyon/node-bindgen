{
  "name": "electron-test",
  "version": "1.0.0",
  "description": "",
  "main": "test.js",
  "scripts": {
    "test": "npm run build && npx electron ./test.js",
    "build": "npm install && npx electron-build-env nj-cli build",
    "start": "npm run test"
  },
  "keywords": [
    "node-bindgen",
    "electron"
  ],
  "author": "fluvio.io",
  "license": "MIT",
  "dependencies": {
    "assert": "^2.0.0",
    "electron": "^18.3.7"
  },
  "devDependencies": {
    "electron-build-env": "^0.2.0"
  }
}
